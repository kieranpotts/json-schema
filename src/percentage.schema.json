{
  "$id": "https://kieranpotts.com/schema/v0/percentage.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Percentage",
  "description": "Percentage value, expressed as a fixed-point signed decimal number.",
  "type": "string",
  "pattern": "^((-?[0-9]+)|(-?([0-9]+)?[.][0-9]+))$"
}
